from setuptools import setup

setup(
   name='filetree',
   version='1.0',
   description='Module for comparing diretories',
   author='chrische-xx',
   packages=['filetree'],
   install_requires=[]
)
